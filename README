## Purpose:

## How to use:

### 1. ✅ Verify Docker Installation
After installing [Docker Desktop](https://www.docker.com/products/docker-desktop/), open a terminal and run the following command to check if Docker is working:

```bash
docker --version
    

## Contact


docker --version

git clone https://github.com/your-username/submission-using-shiny.git
cd submission-using-shiny

docker build -t submission-app .
    success : Successfully tagged submission-app:latest

docker run -p 3838:3838 submission-app

http://localhost:3838
